/**
 * @typedef {Object} GameEntry
 * @property {string} name - Display name (e.g. "1.슈퍼 마리오")
 * @property {string} gamePath - ROM filename (e.g. "Super Mario Bros. (Korea).zip")
 */

/**
 * @typedef {Object} AppState
 * @property {GameEntry[]} games - Current game list
 * @property {string} system - System code (FC, GBA, SFC, MD, GB, GBC, GG, PS, NEOGEO, FBA, CPS)
 */

export {};
